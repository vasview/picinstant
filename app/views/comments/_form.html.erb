<%= form_for [@photo, @photo.comments.build], remote: true do |f| %>
    
    <p>
      <%= f.label :body %><br>
      <%= f.text_area :body %>
      <%= f.hidden_field :user_id, value: current_user.id %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>


